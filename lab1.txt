#include <stdio.h>

using namespace std;

int main()
{
	int num, ed,des,sot,tys;
	scanf("%d", &num);
	ed = num % 10;
	des = (num / 10) % 10;
	sot = (num / 100) % 10;
	tys = (num / 1000) % 10;
	for (int i = 0; i < tys; i++)
		printf ("M");
	if (sot == 9)
		printf ("CM");
	else if (sot >= 5)
		{
			printf("D");
			for(int i = 0; i < sot - 5); i++)
				printf ("C");
		}
	else
		{
			if (sot == 4)
				printf ("CD");
			else
				for (int i = 0; i < sot; i++)
						printf ("C");
					
		}
	
}
